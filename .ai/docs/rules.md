# 📏 Regras de Trabalho e Acordos

## 1. Execução de Comandos
- **Sempre utilize Docker**: Não execute comandos `npm`, `node`, `jest` etc. diretamente no host.
- **Service**: Use `docker compose exec investia-api <comando>` (ou `investia-frontend` conforme o contexto).
- **Exceções**: Apenas comandos de git ou manipulação de arquivos locais (se necessário).

## 2. Padrões de Código
- (Espaço reservado para futuras regras)
